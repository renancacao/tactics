package com.rcacao.tactics.core.data.skill.support

import com.rcacao.tactics.core.data.skill.SkillId

enum class SupportId : SkillId {
    EQUIP_AXES,
    DEFEND,
    JP_BOOST,
    THROW_ITEM,
    SAFE_GUARD,
    REEQUIP,
    EQUIP_CROSSBOW,
    CONCENTRATE
}
